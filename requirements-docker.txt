# Tokenomics Docker Requirements
# Pinned versions for Linux compatibility with LLMLingua

# Core ML/AI dependencies (pinned for compatibility)
numpy==1.24.3
scipy==1.11.4
scikit-learn==1.3.2

# PyTorch (CPU version for container)
torch==2.1.0
--extra-index-url https://download.pytorch.org/whl/cpu

# TensorFlow (needed for some transformers operations)
tensorflow==2.15.0

# Transformers and accelerate (compatible versions)
transformers==4.35.0
accelerate==0.24.0
huggingface-hub==0.19.4

# LLMLingua - the main compression library
llmlingua==0.2.2

# Sentence transformers for embeddings
sentence-transformers==2.2.2

# Vector stores
faiss-cpu==1.7.4
chromadb==0.4.22

# LLM providers
google-cloud-aiplatform>=1.38.0
google-generativeai>=0.3.0
openai>=1.12.0
tiktoken>=0.5.0

# Optimization
ortools>=9.9.0

# Caching
redis>=5.0.0
cachetools>=5.3.0

# Bandit algorithms
mabwiser>=1.12.0

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0
flask>=3.0.0
flask-cors>=4.0.0
tenacity>=8.2.0
requests>=2.31.0

# Logging
structlog>=24.1.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Tokenization
tokenizers>=0.15.0





